# Requirements for PhD Flow Shop project
# Install with: pip install -r requirements.txt
#
# Dependency groups:
#   core         - always needed
#   quantum_qubo - D-Wave neighborhoods (classical + quantum_qubo)
#   qaoa         - gate-based QAOA (src/qaoa/, Willow experiments)

# ---------------------------------------------------------------
# Core
# ---------------------------------------------------------------
matplotlib>=3.9.0
PyYAML>=6.0
scipy>=1.13.0          # COBYLA optimizer for QAOA parameter optimization

# ---------------------------------------------------------------
# Quantum QUBO — D-Wave
# ---------------------------------------------------------------
dimod>=0.12.0
dwave-system>=1.26.0
dwave-cloud-client>=0.14.0
pydantic>=2.0.0        # required by dwave-cloud-client>=0.14.0
                       # NOTE: pydantic 1.x is incompatible — upgrade to 2.x

# ---------------------------------------------------------------
# QAOA — Cirq (gate-based, Willow)
# ---------------------------------------------------------------
cirq>=1.4.0
cirq-google>=1.4.0     # Willow device specs, QVM noise model

# ---------------------------------------------------------------
# Dev / optional
# ---------------------------------------------------------------
# numpy is pulled in transitively by cirq/matplotlib
# but pin explicitly for reproducibility
numpy>=1.26.0
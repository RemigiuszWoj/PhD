Metadata-Version: 2.4
Name: jssp-taillard
Version: 0.1.0
Summary: Minimalne narzędzia do JSSP (Job Shop Scheduling Problem) z parserem instancji Taillarda i prostymi heurystykami.
Author: Remigiusz Wojewódzki
Requires-Python: >=3.9
Description-Content-Type: text/markdown
License-File: LICENSE
Provides-Extra: dev
Requires-Dist: pytest>=7; extra == "dev"
Requires-Dist: pytest-cov>=4; extra == "dev"
Dynamic: license-file

## JSSP (Taillard) – implementacja w repo

Ten folder zawiera minimalną implementację problemu harmonogramowania Job Shop (JSSP):
- parser instancji w formacie Taillarda,
- prostą heurystykę SSGS (greedy),
- testy jednostkowe i prosty CLI.

### Struktura
- `src/jssp/taillard.py` – parser instancji
- `src/jssp/solvers.py` – heurystyka greedy (SSGS)
- `src/jssp/model.py` – proste struktury danych
- `src/jssp/cli.py` – prosty interfejs wiersza poleceń
- `tests/` – testy

### Użycie (dev)
Python >= 3.9.

Instalacja zależności dev:

```bash
python -m venv .venv
source .venv/bin/activate
pip install -e .[dev]
```

Uruchomienie testów:

```bash
pytest
```

CLI:

```bash
python -m jssp path/to/instance.txt
```

Uwagi:
- Parser akceptuje zarówno indeksowanie maszyn 0- jak i 1-bazowe i normalizuje do 0-bazowego.
- Heurystyka `greedy_schedule` implementuje serial schedule generation scheme; nie zapewnia optymalności.
# PhD

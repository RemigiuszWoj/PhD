# Minimal runtime requirements (visualization optional via extra in pyproject)
# Install with: pip install -r requirements.txt

# Core: currently standard library only for algorithms
# Visualization (uncomment if not using extras):
matplotlib>=3.9.0
PyYAML>=6.0
python-dotenv>=1.0

# Quantum stack (D-Wave) — pinned for reproducibility of QPU experiments
dimod==0.12.21
dwave-system==1.34.0

# Quantum stack (IBM / gate-model) — pinned for reproducibility of QAOA experiments
qiskit==2.5.1
qiskit-ibm-runtime==0.48.0
